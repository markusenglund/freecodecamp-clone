const routes = {
  "/": { title: "Home", description: "Should every route have a description?" },
  "/about": {
    title: "About",
    description: "The greatest about page since the birth of Jesus"
  }
};

export default routes;
